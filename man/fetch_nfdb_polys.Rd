% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fire_data.R
\name{fetch_nfdb_polys}
\alias{fetch_nfdb_polys}
\title{Download + load NFDB fire polygons, harmonised + clipped to a study area}
\usage{
fetch_nfdb_polys(
  study_area,
  fire_years = NULL,
  min_size_ha = 1,
  dest = file.path(tempdir(), "NFDB_poly"),
  url = NULL
)
}
\arguments{
\item{study_area}{Study area defining the output CRS + crop extent: a file path (vector or raster),
\code{sf}, \code{SpatVector}, or \code{SpatRaster} (e.g. a simulation \code{flammableMap}).}

\item{fire_years}{Integer vector of fire years to keep; \code{NULL} (default) keeps all years.}

\item{min_size_ha}{Minimum fire size in hectares to keep (default \code{1}); records with a smaller
reported \code{SIZE_HA} are dropped, while records with a missing (\code{NA}) size are always kept. Pass
\code{0} to retain all fires (e.g. when a downstream model filters by size itself).}

\item{dest}{Directory to download + extract the archive into (created if needed). Defaults to a
session tempdir; point it at a persistent (ideally shared) location to cache across runs.}

\item{url}{URL of the NFDB polygon shapefile archive (zip). \code{NULL} (default) uses the CWFIS
current-version archive.}
}
\value{
A \code{SpatVector} of NFDB polygons cropped to \code{study_area}, as \code{\link[=load_nfdb_polys]{load_nfdb_polys()}}.
}
\description{
Downloads the Canadian National Fire Database (NFDB) fire-polygon archive from the CWFIS open-data
server (~780 MB), extracts it, and loads it via \code{\link[=load_nfdb_polys]{load_nfdb_polys()}}. The record ships as several
multi-year partitions with differing columns; all of them are passed to the loader together, which
binds them. Caching, verification and concurrent-worker behaviour are as for \code{\link[=fetch_nfdb_points]{fetch_nfdb_points()}}.
}
\details{
Prefer NBAC (\code{\link[=fetch_nbac_polys]{fetch_nbac_polys()}}); use NFDB polygons only to backfill years NBAC does not cover.
}
\seealso{
\code{\link[=load_nfdb_polys]{load_nfdb_polys()}}

Other fire-record loaders:
\code{\link[=fetch_nbac_polys]{fetch_nbac_polys()}},
\code{\link[=fetch_nfdb_points]{fetch_nfdb_points()}},
\code{\link[=load_nbac_polys]{load_nbac_polys()}},
\code{\link[=load_nfdb_points]{load_nfdb_points()}},
\code{\link[=load_nfdb_polys]{load_nfdb_polys()}}
}
\concept{fire-record loaders}
