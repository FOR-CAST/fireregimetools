% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet.R
\name{write_burn_parquet}
\alias{write_burn_parquet}
\title{Write one replicate's fire table to a partitioned parquet}
\usage{
write_burn_parquet(df, root, replicate)
}
\arguments{
\item{df}{A \code{data.frame} of one replicate's fire records. \code{NULL} or a zero-row
input returns \code{NULL} (nothing is written).}

\item{root}{Directory under which the \verb{replicate=<replicate>} partition is
created (created recursively if needed).}

\item{replicate}{Replicate identifier (integer or string) used for the
partition directory and stamped as the \code{replicate} column.}
}
\value{
The written parquet path (length-1 character), or \code{NULL} for empty
input.
}
\description{
Writes \code{df} (one replicate's fire-size or annual-area table) under
\verb{root/replicate=<replicate>/part-0.parquet}, stamping \code{replicate} as a data
column. The write is atomic on POSIX/NFS: the parquet is written to a unique
temporary name in the destination directory and renamed into place, so a
concurrent reader (or a retried write) never observes a partial file. Many
replicate writers can therefore run concurrently against an NFS output
directory without contention (each writes its own \verb{replicate=} partition).
}
\details{
Run-level identifiers (\code{studyArea}, \code{scenario}, \code{simArea}, ...) should already
be columns of \code{df} so that the summary functions can group by them when a union
of roots is opened together.
}
