% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fire_data.R
\name{load_nfdb_points}
\alias{load_nfdb_points}
\title{Load NFDB fire points, harmonised + clipped to a study area}
\usage{
load_nfdb_points(nfdb_shp, study_area, fire_years = NULL, min_size_ha = 1)
}
\arguments{
\item{nfdb_shp}{Character vector of NFDB point shapefile path(s) (typically the
single \code{NFDB_point} shapefile).}

\item{study_area}{Study area defining the output CRS + crop extent: a file path
(vector or raster), \code{sf}, \code{SpatVector}, or \code{SpatRaster} (e.g. a simulation
\code{flammableMap}). It must carry a CRS. A \strong{vector} study area selects records by
its geometry; a \strong{\code{SpatRaster}} selects by its extent, and its \code{NA} cells do not
narrow that selection -- so an irregular study area passed as a raster also
returns records from the bounding box around it.}

\item{fire_years}{Integer vector of fire years to keep; \code{NULL} (default) keeps all years.}

\item{min_size_ha}{Minimum fire size in hectares to keep (default \code{1}); records
with a smaller reported \code{SIZE_HA} are dropped, while records with a missing
(\code{NA}) size are always kept. Set to \code{0} to keep all reported fire sizes.}
}
\value{
A \code{SpatVector} of NFDB fire points cropped to \code{study_area}, in its CRS,
carrying harmonised integer \code{YEAR} + numeric \code{SIZE_HA} columns.
}
\description{
Loads National Fire DataBase (NFDB) fire-point records (the point shapefile of
fire locations, e.g. \code{NFDB_point}) and harmonises them to the same \code{YEAR} +
\code{SIZE_HA} schema as the perimeter loaders. The year column is detected tolerantly
(\code{YEAR} or \code{FIRE_YEAR}) and the area column from \code{SIZE_HA}, \code{POLY_HA}, or
\code{HECTARES}; records are filtered to \code{fire_years} and \code{SIZE_HA >= min_size_ha},
then projected + cropped to \code{study_area}. Point geometries are always
topologically valid, so (unlike the polygon loaders) no geometry repair is
performed.
}
\details{
NFDB points are fire \emph{locations} rather than mapped burned-area polygons: they
carry reported sizes but no perimeter geometry, while covering fires that were
never delineated. Use this loader when point locations + reported sizes suffice
(e.g. ignition or fire-size-distribution summaries); use the perimeter loaders
(\code{\link[=load_nbac_polys]{load_nbac_polys()}}, \code{\link[=load_nfdb_polys]{load_nfdb_polys()}}) when mapped burned-area geometry is
required. To retain the small fires that are the point record's main advantage,
set \code{min_size_ha = 0} (the default \code{1} ha floor matches the perimeter loaders).
}
\section{Spatial pre-filtering}{

The study-area filter is pushed down to the GDAL/OGR read, so only records near
the study area are materialised in R: the source is opened as a
\code{\link[terra:vect]{terra::vect()}} \emph{proxy} (no geometry read), the study area's footprint is
projected into the records' own CRS as a padded bounding box, and only the
features inside it are read. Geometry repair, projection and the final crop
then see a study-area-sized subset rather than the whole national record, which
is what keeps the loaders' time and memory proportional to the study area.

The box is deliberately generous -- the exact selection is still made by
\code{terra::crop()} in the study area's CRS, and every step of the pushdown falls
back to reading the source whole -- so results do not depend on it. Set
\code{options(fireregimetools.prefilter = FALSE)} to read the sources whole
regardless; the records returned are the same, they just take longer to arrive.
}

\seealso{
Other fire-record loaders:
\code{\link[=fetch_nbac_polys]{fetch_nbac_polys()}},
\code{\link[=fetch_nfdb_points]{fetch_nfdb_points()}},
\code{\link[=fetch_nfdb_polys]{fetch_nfdb_polys()}},
\code{\link[=load_nbac_polys]{load_nbac_polys()}},
\code{\link[=load_nfdb_polys]{load_nfdb_polys()}}
}
\concept{fire-record loaders}
