% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{fire_size_histogram}
\alias{fire_size_histogram}
\title{Fire-size distribution histogram with median-size-per-bin overlay}
\usage{
fire_size_histogram(
  sizes,
  size_col = "size",
  size_unit = "ha",
  binwidth = 0.5,
  title = NULL
)
}
\arguments{
\item{sizes}{A \code{data.frame} of fire records (with column \code{size_col}) or a bare
numeric vector of fire sizes. \code{NULL}, empty, or all-non-positive input returns
\code{NULL}.}

\item{size_col}{Name of the fire-size column when \code{sizes} is a \code{data.frame}
(default \code{"size"}).}

\item{size_unit}{Unit label for the size axis (default \code{"ha"}).}

\item{binwidth}{Width of the \code{log(size)} bins (default \code{0.5}).}

\item{title}{Optional plot title.}
}
\value{
A \code{ggplot} object, or \code{NULL} for empty input.
}
\description{
Draws the fire-size distribution the way the \code{burnSummaries} module does: a
histogram of \verb{log(fire size)} (the number of fires per size bin, left axis)
overlaid with the median \verb{log(fire size)} within each bin (right/secondary
axis). Non-positive and non-finite sizes are dropped.
}
