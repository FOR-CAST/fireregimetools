% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{summarize_fire_sizes}
\alias{summarize_fire_sizes}
\title{Summarize the across-replicate fire-size distribution}
\usage{
summarize_fire_sizes(x, size_col = "size", id_cols = NULL)
}
\arguments{
\item{x}{Parquet paths / roots (see \code{\link[=open_burn_dataset]{open_burn_dataset()}}) or an Arrow
\code{Dataset} / query.}

\item{size_col}{Name of the fire-size column (default \code{"size"}).}

\item{id_cols}{Grouping columns; \code{NULL} (default) auto-detects those present
among \code{simArea}, \code{studyArea}, \code{scenario}, \code{region}.}
}
\value{
A \code{data.frame} with the id columns plus \code{n_fires}, \code{mean}, \code{sd}, \code{min},
\code{q25}, \code{median}, \code{q75}, \code{max}, \code{se}, \code{ci}; zero rows if there is no data.
}
\description{
Opens the per-replicate fire-event parquet(s) lazily and pushes the reduction
down to Arrow compute, so the per-event rows are never all held in memory at
once. All fire events in each identifier group (pooled across replicates and,
by default, across years) contribute to the distribution: the count and the
five-number summary (\code{min}, \code{q25}, \code{median}, \code{q75}, \code{max}) of \code{size_col} plus
the mean, sd, and standard error / 95\% confidence half-width. The quantiles and
median are Arrow-approximate; \code{min}/\code{max}/\code{mean}/\code{sd}/\code{n_fires} are exact.
}
