% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fire_data.R
\name{fetch_nfdb_points}
\alias{fetch_nfdb_points}
\title{Download + load NFDB fire points, harmonised + clipped to a study area}
\usage{
fetch_nfdb_points(
  study_area,
  fire_years = NULL,
  min_size_ha = 1,
  dest = file.path(tempdir(), "NFDB_point"),
  url = NULL
)
}
\arguments{
\item{study_area}{Study area defining the output CRS + crop extent: a file path (vector or raster),
\code{sf}, \code{SpatVector}, or \code{SpatRaster} (e.g. a simulation \code{flammableMap}). It must carry a CRS. A
\strong{vector} study area selects records by its geometry; a \strong{\code{SpatRaster}} selects by its extent,
and its \code{NA} cells do not narrow that selection -- so an irregular study area passed as a raster
also returns records from the bounding box around it.}

\item{fire_years}{Integer vector of fire years to keep; \code{NULL} (default) keeps all years.}

\item{min_size_ha}{Minimum fire size in hectares to keep (default \code{1}); records with a smaller
reported \code{SIZE_HA} are dropped, while records with a missing (\code{NA}) size are always kept. Pass
\code{0} to retain all fires (e.g. when a downstream model filters by size itself).}

\item{dest}{Directory to download + extract the archive into (created if needed). Defaults to a
session tempdir; point it at a persistent (ideally shared) location to cache across runs.}

\item{url}{URL of the NFDB point shapefile archive (zip). \code{NULL} (default) uses the CWFIS
current-version archive.}
}
\value{
A \code{SpatVector} of NFDB fire points cropped to \code{study_area}, as \code{\link[=load_nfdb_points]{load_nfdb_points()}}.
}
\description{
Downloads the Canadian National Fire Database (NFDB) fire-point archive from the CWFIS open-data
server (~42 MB), extracts it, and loads it via \code{\link[=load_nfdb_points]{load_nfdb_points()}} (same \code{YEAR} + \code{SIZE_HA}
harmonisation and study-area clipping, with the NFDB \code{CAUSE} column and other source attributes
preserved).
}
\details{
The archive is downloaded + extracted once per \code{dest} and reused afterwards, so repeated runs --
and concurrent workers sharing a \code{dest} on shared storage, which coordinate via a lock -- do not
re-fetch it. A cached extraction is reused only once it has been verified complete against the
archive's own manifest, since a download or extraction interrupted partway leaves plausible-looking
short files behind that a reader will happily accept.
}
\seealso{
\code{\link[=load_nfdb_points]{load_nfdb_points()}}

Other fire-record loaders:
\code{\link[=fetch_nbac_polys]{fetch_nbac_polys()}},
\code{\link[=fetch_nfdb_polys]{fetch_nfdb_polys()}},
\code{\link[=load_nbac_polys]{load_nbac_polys()}},
\code{\link[=load_nfdb_points]{load_nfdb_points()}},
\code{\link[=load_nfdb_polys]{load_nfdb_polys()}}
}
\concept{fire-record loaders}
