% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{fire_cycle}
\alias{fire_cycle}
\title{Fire cycle from a per-replicate annual-area-burned table}
\usage{
fire_cycle(x, area_col = "area_ha", landscape_ha, id_cols = NULL)
}
\arguments{
\item{x}{Parquet paths / roots (see \code{\link[=open_burn_dataset]{open_burn_dataset()}}) or an Arrow
\code{Dataset} / query.}

\item{area_col}{Name of the annual-area-burned column (default \code{"area_ha"}).}

\item{landscape_ha}{Total flammable/active landscape area, in hectares.}

\item{id_cols}{Grouping columns; \code{NULL} (default) auto-detects those present
among \code{simArea}, \code{studyArea}, \code{scenario}.}
}
\value{
A \code{data.frame} with the id columns plus \code{mean_annual_area} and
\code{fire_cycle_yr}; zero rows if there is no data.
}
\description{
The fire cycle is the number of years for an area equal to the whole landscape
to burn: \code{landscape_ha / mean(area_col)}, where the mean is taken over all rows
(pooling replicates and years) within each identifier group. Pre-filter the
input (e.g. to a single region) before calling if needed.
}
