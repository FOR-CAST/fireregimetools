% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fire_data.R
\name{load_nfdb_polys}
\alias{load_nfdb_polys}
\title{Load NFDB fire polygons, harmonised + clipped to a study area}
\usage{
load_nfdb_polys(nfdb_shp, study_area, fire_years)
}
\arguments{
\item{nfdb_shp}{Character vector of NFDB polygon shapefile path(s).}

\item{study_area}{Study area defining the output CRS + crop extent: a file path
(vector or raster), \code{sf}, \code{SpatVector}, or \code{SpatRaster}.}

\item{fire_years}{Integer vector of fire years to keep.}
}
\value{
A \code{SpatVector} of NFDB polygons cropped to \code{study_area}, in its CRS,
carrying harmonised integer \code{YEAR} + numeric \code{SIZE_HA} columns.
}
\description{
Loads National Fire DataBase (NFDB) fire polygons and harmonises them to the
same \code{YEAR} + \code{SIZE_HA} schema as \code{\link[=load_nbac_polys]{load_nbac_polys()}}. The year column is
detected tolerantly (\code{YEAR} or \code{FIRE_YEAR}) and the area column from \code{SIZE_HA},
\code{POLY_HA}, or \code{HECTARES}. Invalid geometries are dropped, records are filtered
to \code{fire_years} and \code{SIZE_HA >= 1} ha, then projected + cropped to \code{study_area}.
The NFDB poly record ships multiple multi-year partitions with differing
columns, so pass all their paths together.
}
\details{
Prefer NBAC (\code{\link[=load_nbac_polys]{load_nbac_polys()}}); use NFDB polygons only to backfill years NBAC
does not cover.
}
\seealso{
Other fire-perimeter loaders:
\code{\link[=load_nbac_polys]{load_nbac_polys()}}
}
\concept{fire-perimeter loaders}
