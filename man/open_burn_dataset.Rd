% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet.R
\name{open_burn_dataset}
\alias{open_burn_dataset}
\title{Open replicate fire parquet(s) as one lazy Arrow dataset}
\usage{
open_burn_dataset(x)
}
\arguments{
\item{x}{Character vector of parquet file paths and/or directories; directories
are scanned recursively for \verb{*.parquet}.}
}
\value{
An Arrow \code{Dataset} (lazy), or \code{NULL} if no parquet files are found.
}
\description{
Resolves \code{x} -- parquet file paths and/or dataset roots -- to a flat list of
\verb{*.parquet} files and opens them as a single lazy \code{\link[arrow:open_dataset]{arrow::open_dataset()}}
\code{Dataset}. Because \code{replicate} and any run-level identifiers are stored as data
columns (not inferred from the directory tree), files from several roots open
together without a \code{UnionDataset}, and opening from explicit file paths avoids
depending on NFS directory-listing freshness.
}
