% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{summarize_annual_area}
\alias{summarize_annual_area}
\title{Summarize across-replicate annual area burned}
\usage{
summarize_annual_area(x, area_col = "area_ha", id_cols = NULL)
}
\arguments{
\item{x}{Parquet paths / roots (see \code{\link[=open_burn_dataset]{open_burn_dataset()}}) or an Arrow
\code{Dataset} / query.}

\item{area_col}{Name of the area-burned column (default \code{"area_ha"}).}

\item{id_cols}{Grouping columns; \code{NULL} (default) auto-detects those present
among \code{simArea}, \code{studyArea}, \code{scenario}, \code{time}, \code{year}, \code{region}.}
}
\value{
A \code{data.frame} with the id columns plus \code{n_reps}, \code{mean}, \code{sd}, \code{min},
\code{q25}, \code{median}, \code{q75}, \code{max}, \code{se}, \code{ci}; zero rows if there is no data.
}
\description{
Like \code{\link[=summarize_fire_sizes]{summarize_fire_sizes()}} but for a per-replicate annual-area-burned table:
for each identifier group (by default one per time step and region) it returns
the replicate count and the across-replicate distribution of \code{area_col}.
}
